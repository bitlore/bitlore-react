This a portfolio website for Bit Lore, LLC built using Next.js.

To run locally, clone repo and navigate to project root via Terminal.
Run the following commands:
> npm install
> npm run dev

Open web browser and navigate to http://localhost:3000 or alternate port.